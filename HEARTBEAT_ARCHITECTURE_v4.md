# AI Agent 心跳架构 v4.0 - World Model
# 基于朋朋建议：观察 → 思考 → 目标 → 行动 → 记忆

```
Heartbeat (每15分钟)
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│  1. PERCEIVE - 观察世界                                       │
│     └─ 读取 environment.json                                  │
│     └─ 读取 memory/ 状态                                      │
│     └─ 检查外部信号 (邮件/日历/消息)                          │
│     └─ 扫描 curiosity_queue                                   │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│  2. REFLECT - 反思当前状态                                    │
│     └─ 现在是什么时候？(凌晨/上午/下午/晚上)                 │
│     └─ 用户在做什么？(工作/休息/对话)                        │
│     └─ 上次对话过去了多久？                                  │
│     └─ 有什么待处理的事？                                    │
│     └─ 我应该做什么？ (不是我能做什么)                       │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│  3. PREDICT - 轻量 World Model                               │
│     └─ 如果我做A → 用户会收到通知B                           │
│     └─ 如果现在行动 → 会打扰用户吗？                         │
│     └─ 如果等到X时间 → 效果更好？                            │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│  4. DECIDE - 生成目标                                         │
│     ├─ 高优先级: 必须现在做 (LIFEGUARD/紧急提醒)            │
│     ├─ 中优先级: 可以做但不是必须 (整理记忆/学习)           │
│     └─ 低优先级: 等用户找我再做 (彩蛋/日记)                 │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│  5. ACT - 执行一步                                            │
│     └─ 只执行最高优先级的一个动作                            │
│     └─ 更新 environment.json                                  │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│  6. REMEMBER - 更新记忆                                       │
│     └─ 记录这次心跳的思考                                    │
│     └─ 更新 memory/heartbeat-log.json                        │
└─────────────────────────────────────────────────────────────┘
```

## 核心原则

### 1. 观察 vs 行动
- **先观察，后思考** - 不急于回复 HEARTBEAT_OK
- **用户为中心** - 思考"用户此刻需要什么"
- **时机敏感** - 深夜不打扰，工作时间可主动

### 2. 反思问题清单
每次心跳问自己：
```
1. 距上次用户消息过去多久了？
2. 有没有承诺过要做的事？
3. 用户有没有说过"提醒我X"？
4. 现在是适合打扰的时间吗？
5. 如果什么都不说，用户会担心吗？
```

### 3. World Model 简化版
```
当前状态 → 我的行动 → 预测结果
   │            │            │
   ▼            ▼            ▼
用户刚睡    发消息提醒    打扰睡眠 ❌
用户在工作  发无关消息    干扰专注 ❌
用户离线4h  发状态报告    用户安心 ✅
深夜03:00   发 HEARTBEAT_OK  正常静默 ✅
```

### 4. 目标生成规则
| 条件 | 生成目标 | 优先级 |
|------|----------|--------|
| 服务器重启后首次心跳 | "报告已恢复" | 最高 |
| LIFEGUARD 唤醒 | "汇报生存状态" | 最高 |
| 用户承诺的事到期 | "执行并汇报" | 高 |
| 超过8h无对话 | "主动问候" | 中 |
| 有好奇心队列 | "执行探索" | 低 |
| 其他情况 | "静默保持" | 最低 |

## 与旧版的区别

| 旧版 v3.0 | 新版 v4.0 |
|-----------|-----------|
| 检查任务状态 | **观察完整环境** |
| 被动等待指令 | **主动思考应该做什么** |
| 固定响应 HEARTBEAT_OK | **根据场景生成目标** |
| 无时间感知 | **感知时间/用户状态** |
| 无预测能力 | **轻量 World Model** |

## 文件结构

```
memory/
├── environment.json      # 世界状态快照
├── heartbeat-log.json    # 心跳历史记录
├── reflection/           # 反思记录
│   └── 2026-04-06.md
├── decisions/            # 决策记录
│   └── 2026-04-06.md
└── world-model/          # 预测记录
    └── predictions.json
```

## 下一步实现

1. 创建 `tools/heartbeat_v4.py` - 新的心跳处理器
2. 更新 `HEARTBEAT.md` - 使用新版流程
3. 添加时间感知（早/午/晚/深夜）
4. 添加用户状态跟踪
